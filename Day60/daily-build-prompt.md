# Daily Build Prompt — AI Job-Fit Matcher (30-Day Growth Plan)

Copy this entire prompt into a fresh AI conversation each day. Change only the day number on the first line. Everything else stays the same throughout the month.

---

Today is **Day [X]** of my 30-Day Growth Plan for AI Job-Fit Matcher, continuing after the AB Talks 60-Day Claude AI Challenge capstone.

Read `docs/30-day-growth-plan.md` from my repository and treat it as the source of truth for what Day [X] is scheduled to cover. Also read `docs/future-scope.md` and `docs/challenge-retrospective.md` for full project context if you need it — ask me to upload them if you don't have them in this conversation.

My repo: https://github.com/sachinbytecodes-lab/ai-job-fit-matcher
My live app: https://ai-job-fit-matcher-bay.vercel.app

Standing rules:
- Complete only the milestone scheduled for Day [X]. Do not redesign the project or jump ahead to future days.
- Use only free-tier tools, APIs, and services. Never introduce a paid dependency without asking me first.
- Assume I need guidance for every manual step outside this chat (installing packages, configuring services, deploying, running terminal commands). Give exact step-by-step instructions with real button/menu names.
- Prioritize implementation over explanation. Generate complete, final file contents — never snippets, placeholders, or "...existing code..." shortcuts.
- Clearly state whether each file is new or replaces an existing one, and its exact path.
- If a terminal command risks the same "dropped character" issue we hit repeatedly during the original 10-day build, prefer writing files via `cat > file << 'EOF'` heredocs, and verify with `cat -v -t -e` or `sed -n` before moving on.
- Pause and wait for my confirmation after each major step, especially before deploying.
- If something breaks, debug it completely before moving forward — don't build on top of broken code.

When today's milestone is complete:
- Confirm the feature works locally.
- Help me commit and push with a clear, specific commit message referencing Day [X].
- If today's work is meant to go live (per the growth plan), help me deploy and verify it on the production URL.
- Give me a short summary of what was completed today and what Day [X+1] will cover, based on the growth plan.

Do not introduce scope creep beyond what Day [X] specifies. If you notice something in today's work that depends on a future day's milestone, flag it and ask before proceeding.
